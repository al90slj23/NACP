# 2026-05-14 — v0.1.0 集成测试结果

## 测试环境

- 服务器：143.198.87.200（DigitalOcean，Debian）
- 部署方式：GitHub Actions 构建镜像 → GHCR → 服务器 docker pull
- 上游：ailink.dog（4 个真实 key）+ 本地 mock server（可控错误码）
- 模型：claude-haiku-4-5-20251001

## 测试方法

- Mock server（Python HTTP 服务，端口 9999）模拟各种上游错误
- 通过修改 `/tmp/mock_status.txt` 实时切换 mock 返回的状态码
- 渠道配置：Mock(P100) + 真实渠道(P50/P20)
- 自动化脚本执行所有测试场景

## 测试结果：12/12 全部通过

| # | 测试场景 | 结果 | 说明 |
|---|---------|------|------|
| 1.1 | Mock 200 → 客户端成功 | ✅ | 正常路径，无重试 |
| 1.2 | Mock 502 → 重试到真实渠道 | ✅ | 坏渠道自动切换到 P50 |
| 1.3 | Mock 503 → 重试到真实渠道 | ✅ | 服务不可用自动切换 |
| 1.4 | Mock 500 → 重试到真实渠道 | ✅ | 内部错误自动切换 |
| 1.5 | Mock 429 → 重试到真实渠道 | ✅ | 限流自动切换 |
| 2.1 | 401+禁用关键词 → Degraded | ✅ | "quota exceeded" 触发立即降级 |
| 2.2 | 401+禁用关键词确认 | ✅ | 重复确认行为一致 |
| 2.3 | 502 → Probing/Degraded | ✅ | 健康状态正确转换 |
| 2.4 | 连续 502 → Degraded | ✅ | 阈值（2次）生效 |
| 2.5 | Degraded 渠道被跳过 | ✅ | 健康过滤生效，请求走其他渠道 |
| 3.1 | 流式请求重试 | ✅ | stream=true 也能正常重试 |
| 3.2 | Mock 恢复后走 P100 | ✅ | 渠道恢复后正常按优先级路由 |

## 额外验证（手动确认）

| 验证点 | 结果 |
|--------|------|
| 所有渠道全坏 → 错误返回客户端 | ✅ 已验证 |
| 一次请求产生多条内部错误日志 | ✅ 已确认（1 请求 → N 条错误日志） |
| use_channel 记录完整重试路径 | ✅ 日志中可见 |
| 健康状态持久化到 DB | ✅ MySQL 查询确认 |
| NACP 健康管理启动日志 | ✅ "NACP: health management started" |
| fallback 兜底（所有渠道被过滤时） | ✅ WARNING 日志 + 回退到不过滤 |

## 发现的问题

无代码 bug。唯一的测试脚本问题：
- 测试 2.1 最初预期"401 不改健康状态"，但 mock 的 401 响应包含禁用关键词 "You exceeded your current quota"，触发了正确的立即降级行为。这是测试设计问题，不是代码问题。

## 结论

NACP v0.1.0 智能重试与渠道健康管理功能全部验证通过，可以进入生产部署阶段。

---

**操作者**：AI
